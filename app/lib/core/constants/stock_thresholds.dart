/// A product at or below this count is flagged low-stock. Shared by the
/// Sell screen's product tiles and the Stock screen so both agree on what
/// "low" means — one constant, not two copies that could drift apart.
const kLowStockThreshold = 5;
